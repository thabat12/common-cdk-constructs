# CDK Fargate Scaffold Environment Configuration
# Copy this file to .env and customize the values

# =============================================================================
# REQUIRED: Application Configuration
# =============================================================================

# The name of your application (used for resource naming)
APP_NAME=my-awesome-app

# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=123456789012

# =============================================================================
# OPTIONAL: Docker Configuration
# =============================================================================

# Docker image to deploy (defaults to APP_NAME if not specified)
# Can be:
# - ECR repository name (e.g., my-app)
# - Full ECR URI (e.g., 123456789012.dkr.ecr.us-east-1.amazonaws.com/my-app)
# - Docker Hub image (e.g., nginx:latest)
DOCKER_IMAGE=my-awesome-app

# =============================================================================
# OPTIONAL: Fargate Configuration
# =============================================================================

# Fargate task CPU units (256 = 0.25 vCPU, 512 = 0.5 vCPU, 1024 = 1 vCPU, etc.)
# Valid values: 256, 512, 1024, 2048, 4096
FARGATE_CPU=256

# Fargate task memory in MiB
# Valid values: 512, 1024, 2048, 3072, 4096, 5120, 6144, 7168, 8192
FARGATE_MEMORY=512

# Desired number of tasks
DESIRED_COUNT=1

# Maximum number of tasks for auto-scaling
AUTO_SCALING_MAX_CAPACITY=5

# =============================================================================
# OPTIONAL: Monitoring Configuration
# =============================================================================

# Log retention days (1, 3, 5, 7, 14, 30, 60, 90, 120, 150, 180, 365, 400, 545, 731, 1827, 3653)
LOG_RETENTION_DAYS=7

# =============================================================================
# OPTIONAL: Cost Management
# =============================================================================

# Cost center for resource tagging and cost allocation
COST_CENTER=development

# =============================================================================
# OPTIONAL: Environment Override
# =============================================================================

# Override the environment (dev, staging, production)
# If not set, will use the --context environment flag or default to 'dev'
ENVIRONMENT=dev

# =============================================================================
# OPTIONAL: Advanced Configuration
# =============================================================================

# Custom tags to apply to all resources (comma-separated key=value pairs)
# Example: CUSTOM_TAGS=Team=Engineering,Owner=DevOps,Project=WebApp
CUSTOM_TAGS=

# =============================================================================
# EXAMPLES FOR DIFFERENT ENVIRONMENTS
# =============================================================================

# Development Environment
# APP_NAME=my-app-dev
# FARGATE_CPU=256
# FARGATE_MEMORY=512
# DESIRED_COUNT=1
# AUTO_SCALING_MAX_CAPACITY=2
# LOG_RETENTION_DAYS=7
# COST_CENTER=dev

# Staging Environment
# APP_NAME=my-app-staging
# FARGATE_CPU=512
# FARGATE_MEMORY=1024
# DESIRED_COUNT=2
# AUTO_SCALING_MAX_CAPACITY=5
# LOG_RETENTION_DAYS=14
# COST_CENTER=staging

# Production Environment
# APP_NAME=my-app-prod
# FARGATE_CPU=1024
# FARGATE_MEMORY=2048
# DESIRED_COUNT=3
# AUTO_SCALING_MAX_CAPACITY=10
# LOG_RETENTION_DAYS=30
# COST_CENTER=production

# =============================================================================
# NOTES
# =============================================================================

# 1. Copy this file to .env: cp env.example .env
# 2. Update the values according to your needs
# 3. Never commit .env files to version control
# 4. Use different .env files for different environments if needed
# 5. The scaffold will automatically use the appropriate configuration based on the environment context
